import{getUser}from"@/lib/auth";import{redirect}from"next/navigation";import{loginAction}from"../actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){if(await getUser())redirect("/");const q=await searchParams;return <main className="login"><section className="loginCard"><div className="brandMark">Л</div><p className="eyebrow">НОНВОЙХОНАИ ЛОЧАРИ</p><h1>Добро пожаловать</h1><p className="muted">Войдите, чтобы открыть учёт пекарни</p>{q.error&&<div className="error">Неверный логин или пароль</div>}<form action={loginAction} className="form"><label>Логин<input name="username" required autoComplete="username"/></label><label>Пароль<input name="password" type="password" required autoComplete="current-password"/></label><button>Войти в систему</button></form></section></main>}
